Grey Panther's static site
===

Setup:

* `virtualenv venv`
* `pip install -r requirements.txt`
* `svn export https://github.com/getpelican/pelican-themes/trunk/backdrop`
* `pelican-themes -i backdrop`
* `rm -r backdrop`

License: CC-BY Unported 3.0 (see LICENSE). 
